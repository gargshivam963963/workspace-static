import StepCard from "../StepCard"

const ForgotPassword = () => {

    return (
        <StepCard heading="Almost there! " subheading="Open your email and follow the link to complete your account activation." onClick={() => { }} btnText="Go to Email">
        </StepCard>
    )
}

export default ForgotPassword